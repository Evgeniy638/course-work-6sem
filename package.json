{
    "name": "root",
    "private": true,
    "scripts": {
        "kill-ports": "kill-port --port 8080,3000",
        "start": "npm run kill-ports && lerna run start --parallel"
    },
    "devDependencies": {
        "kill-port": "^1.6.1",
        "lerna": "^4.0.0"
    }
}
